#include <bits/stdc++.h>
using namespace std;

int n, nums[10005];
int cnt = 1, j, i;
int matrix[102][102];

bool cmp(int a, int b) {
    return a > b;
}

int main(void) {
    int a;
    scanf("%d", &a);
    cout<<a<<endl;
    return 0;
}
